var tools = require("./testtools");
var expect = tools.expect;

var Pillbox = require("..");
var Pill = require("../lib/pill");

describe("Pill", function () {
  describe("constructor", function () {
    it("should create a new Pill", function () {
      //
    });

    it("should initialize with no states if none were given", function () {
      //
    });

    it("should add states that were passed to the constructor", function () {
      //
    });
  });
});
